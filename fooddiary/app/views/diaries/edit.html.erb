

 
<%= form_with(model: @diary, local: true, multipart: true) do |form| %>
 
  <% if @diary.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@diary.errors.count, "error") %> 에러
      </h2>
      <ul>
        <% @diary.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div class="navbar">
  <a href="#home">1</a>
  <a href="#news">2</a>
  <a href="#contact">3</a>
</div>

<div class="row">
  <div class="column side"></div>
  <div class="column middle">  
<br>
<br>
<br>
<div class="bg-text"><%= render 'form' %></div>

<div class="tb">  
<h1>다이어리 수정</h1>
  <p>
    <%= form.label :음식이름 %><br>
    <%= form.text_field :foodname %>
  </p>
 
  <p>
    <%= form.label :음식사진 %><br>
    <%= form.file_field :image %><br>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', diaries_path %>
</div>